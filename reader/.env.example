DATABASE_URL="file:./dev.db"

# Path to the course collection to seed, relative to reader/.
# Only required when running db:seed or db:reset.
# Must point to a folder inside ../courses/ that contains a manifest.json
# and the markdown entry file specified in that manifest.
# Not used at runtime — the active course is stored in the database.
NOTES_PATH="../courses/<your-collection>"
